module.exports = class Schools extends schoolsBaseController {
  constructor() {
    super();
  }

  find(req) {
    return super.find(req);
  }

  assessments(req) {
    return super.assessments(req);
  }

};